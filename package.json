{
  "name": "zain-alvi-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173 --bind 127.0.0.1 --directory dist",
    "check": "node --check dist/app.js && node --check dist/content.js && node --check dist/navigation.js",
    "test": "node --test tests/navigation.test.mjs",
    "test:unit": "node --test tests/navigation.test.mjs",
    "test:browser": "node tests/interaction.mjs"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
